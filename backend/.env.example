# =========================================================================
# Copy this file to `.env` (same folder) and fill in the real values.
# `.env` is gitignored — never commit it or paste real secrets into chat/PRs.
# =========================================================================

# --- Google OAuth client (from Google Cloud Console > APIs & Services > Credentials) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback
GOOGLE_SCOPES=openid email profile https://www.googleapis.com/auth/calendar https://www.googleapis.com/auth/gmail.send

# --- Role assignment ---
# No shared company domain, so internal staff (using their personal Gmail accounts)
# are listed explicitly as "email:role" pairs, comma-separated. Roles must be one of:
# recruiter, hiring_manager, interviewer. Anyone else needs a valid interview invite
# token to sign in (candidates authenticate via a per-interview invite link, not a
# bare login button) — an email not listed here and without an invite is rejected.
ALLOWED_INTERNAL_USERS=you@gmail.com:recruiter,teammate@gmail.com:interviewer

# --- Secrets you generate locally (never reuse these across dev/prod) ---
# 32-byte AES-256 key, base64-encoded — encrypts Google tokens at rest in the DB.
# Generate with: python -c "import os,base64; print(base64.b64encode(os.urandom(32)).decode())"
TOKEN_ENC_KEY=
# Long random string used to sign session JWTs and the OAuth 'state' param.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SESSION_JWT_SECRET=

# --- Database ---
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/scheduler
# Quick local start without installing Postgres:
# DATABASE_URL=sqlite:///./dev.db

# --- Email (app/notifications/service.py) ---
# Get a key at resend.com/api-keys. Blank = notifications still record to the
# in-app inbox, just skip the real send (logged, never a hard failure).
RESEND_API_KEY=
# "Name <address>". The default (resend.dev) needs no domain verification but
# only delivers to the Resend account owner's own email — verify a real
# domain in the Resend dashboard and put its address here before relying on
# this for real candidate/interviewer recipients.
RESEND_FROM_EMAIL=WorkHire <onboarding@resend.dev>

# --- App ---
FRONTEND_URL=http://localhost:5173
# false for local http dev, true once served over https in any deployed environment
SESSION_COOKIE_SECURE=false
# "lax" when frontend+backend are the same site (localhost:5173 + :8000 count
# as one - port doesn't matter). Set to "none" the moment they're on two
# different hosts (e.g. two separate tunnel subdomains, or a real prod
# split) - otherwise the session cookie never reaches the frontend's
# fetch("/auth/me") and login looks like it silently fails. "none" requires
# SESSION_COOKIE_SECURE=true (SameSite=None requires Secure regardless).
SESSION_COOKIE_SAMESITE=lax
SESSION_TTL_MINUTES=60
