[pytest]
; Lets `import app.scheduling...` resolve the same way it does for the rest
; of the FastAPI app when pytest is run from backend/ — without this, pytest
; would insert backend/tests/scheduling/ itself onto sys.path (since neither
; tests/ nor tests/scheduling/ has an __init__.py) instead of backend/.
pythonpath = .
testpaths = tests
